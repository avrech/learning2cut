# SPECIFIY HERE THE HPARAMS TO SWEEP:
sweep:
  # INSTANCE PARAMETERS
  graph_idx:
    search: grid_range
    range: 10
  scip_seed:
    search: grid
    values: [223, 100, 521, 46, 52] #, 14, 176, 80, 174, 198]

  # MCCORMIC_CYCLE_SEPARATOR PARAMETERES
  max_per_root:
    search: grid
    values: [0, 1000000] # in practice it is disable / enable. 0 must be the first in list for experiment control reason
  max_per_round:
    search: grid
    values: [0.1, 0.5, 1]
  criterion:
    search: grid
    values: [most_violated_cycle] #random, 
#  max_per_node:
#    search: grid
#    values: [10, 50, 100]
#  max_per_round_relative_to:
#    search: grid
#    values: [num_vars] #, 'num_violations'] #, num_fractions
#  forcecut:
#    search: grid
#    values: [True, False]

  objparalfac:
    search: grid
    values: [0.1, 0.2]
  dircutoffdistfac:
    search: grid
    values: [0.5, 0.6]
  efficacyfac:
    search: grid
    values: [1, 0.8, 1.2]
  intsupportfac:
    search: grid
    values: [0.1, 0.2]
  maxrounds:
    search: grid
    values: [-1]
  maxroundsroot:
    search: grid
    values: [-1]
  maxcuts:
    search: grid
    values: [1, 4, 16, 32]
  maxcutsroot:
    search: grid
    values: [2000]





# SPECIFY HERE OTHER PARAMETERS TO OVERRIDE DEFAULTS
constants:
  # GENERAL EXPERIMENT PARAMETERS
  barabasi_albert_m: 10
  weights: ones  # uniform01 | normal | ones
  graph_type: barabasi-albert
  time_limit_sec: 600
  dataset_generation_seed: 100
  graph_size: 60

  # MCCORMIC_CYCLE_SEPARATOR PARAMETERS
#  max_per_node: 1000
  max_per_round: 1
#  max_per_round_relative_to: num_vars
  criterion: most_infeasible_var
#  max_per_root: 2000

  # SCIP SEPARATING PARAMETERS
#  objparalfac: 0.1       # default
#  dircutoffdistfac: 0.5  # default
#  efficacyfac: 1         # default
#  intsupportfac: 0.1     # default
#  maxrounds: -1          # default
#  maxroundsroot: -1       # default
#  maxcuts: 100           # default
#  maxcutsroot: 2000       # default

