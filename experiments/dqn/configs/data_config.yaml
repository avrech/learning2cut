# DATASETS
# measured lp_iterations for SCIP solving at the root node only:
# barabasi albert 25 (10000 instances) mean ~ 1.9K, std < 900
# barabasi albert 50 (20 instances) mean ~ 36K, std < 7K
# barabasi albert 100 (20 instances) mean ~ 80K, std ~ 51K
# so we set the lp_iterations_limit to 3K, 50K and 100K for the graphs of 25, 50 and 100 nodes respectively
datasets:
  trainset_20_30:
    lp_iterations_limit: 3000
    ngraphs: 10000
    barabasi_albert_m: 10
    weights: normal  # uniform01 | normal | ones
    graph_type: barabasi-albert
    seed: 46
    graph_size:
      min: 20
      max: 30
    dataset_name: trainset.20-30
    time_limit_sec: 600
    baseline_solver: scip
    save_all_stats: False
  validset_20_30:
    lp_iterations_limit: 3000
    eval_interval: 1  # single-thread: 100, distributed: 1
    scip_seed: [52, 176, 223]
    ngraphs: 10
    barabasi_albert_m: 10
    weights: normal  # uniform01 | normal | ones
    graph_type: barabasi-albert
    seed: 46
    graph_size:
      min: 20
      max: 30
    dataset_name: validset.20-30
    time_limit_sec: 600
    baseline_solver: scip
    save_all_stats: True
  testset_20_30:
    lp_iterations_limit: 3000
    eval_interval: 1  # single-thread: 100, distributed: 1
    scip_seed: [52, 176, 223]
    ngraphs: 10
    barabasi_albert_m: 10
    weights: normal  # uniform01 | normal | ones
    graph_type: barabasi-albert
    seed: 46
    graph_size:
      min: 20
      max: 30
    dataset_name: testset.20-30
    time_limit_sec: 600
    baseline_solver: scip
    save_all_stats: True
  validset_50_60:
    lp_iterations_limit: 10000  # 50000
    eval_interval: 10  # single-thread: 500, distributed: 10
    scip_seed: [52, 176, 223]
    ngraphs: 10
    barabasi_albert_m: 10
    weights: normal  # uniform01 | normal | ones
    graph_type: barabasi-albert
    seed: 46
    graph_size:
      min: 50
      max: 60
    dataset_name: validset.50-60
    time_limit_sec: 3600
    baseline_solver: scip
    save_all_stats: True
  testset_50_60:
    lp_iterations_limit: 10000  # 50000
    eval_interval: 10  # single-thread: 500, distributed: 10
    scip_seed: [52, 176, 223]
    ngraphs: 10
    barabasi_albert_m: 10
    weights: normal  # uniform01 | normal | ones
    graph_type: barabasi-albert
    seed: 46
    graph_size:
      min: 50
      max: 60
    dataset_name: testset.50-60
    time_limit_sec: 3600
    baseline_solver: scip
    save_all_stats: True
  validset_90_100:
    lp_iterations_limit: 30000  # 100000
    eval_interval: 50  # single-thread: 1000, distributed: 50
    scip_seed: [52, 176, 223]
    ngraphs: 10
    barabasi_albert_m: 10
    weights: normal  # uniform01 | normal | ones
    graph_type: barabasi-albert
    seed: 46
    graph_size:
      min: 90
      max: 100
    dataset_name: validset.90-100
    time_limit_sec: 3600
    baseline_solver: scip
    save_all_stats: True
  testset_90_100:
    lp_iterations_limit: 30000  # 100000
    eval_interval: 50  # single-thread: 1000, distributed: 50
    scip_seed: [52, 176, 223]
    ngraphs: 10
    barabasi_albert_m: 10
    weights: normal  # uniform01 | normal | ones
    graph_type: barabasi-albert
    seed: 46
    graph_size:
      min: 90
      max: 100
    dataset_name: testset.90-100
    time_limit_sec: 3600
    baseline_solver: scip
    save_all_stats: True
